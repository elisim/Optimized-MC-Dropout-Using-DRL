from setuptools import setup

setup(name='gym_eli',  # package name. used for import your environment (import gym_eli)
      version='0.0.2',
      install_requires=['gym']  # TODO: maybe add here 'tqdm', and other stuff you use
      )
